//this is actually a example user config, it only extends (and propably overwrites) some values

module.exports = {
    database: {
        type: 'real', //mock|real
         host: 'localhost',
         port: 6379,
         userName: 'root',
         password: ''
    }
};